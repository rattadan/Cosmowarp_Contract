[package]
name = "dapp_registry"
version = "0.1.0"
authors = ["nRide Team"]
edition = "2021"
description = "On-chain dApp registry (title, descriptions, logos, social links)"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[features]
default = []
library = []

[dependencies]
cosmwasm-std = { version = "1.5", features = ["staking"] }
cosmwasm-schema = "1.5"
cw-storage-plus = "1.2"
cw2 = "1.1"
schemars = "0.8"
serde = { version = "1.0", default-features = false, features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
cosmwasm-vm = "1.5"

[profile.release]
opt-level = 3
debug = false
rpath = false
lto = true
debug-assertions = false
codegen-units = 1
panic = "abort"
overflow-checks = true
